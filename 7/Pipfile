# Use pipenv graph to see installed packages and their dependencies

[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
pillow = "*"

[requires]
python_version = "3.6"

[pipenv]
allow_prereleases = true
